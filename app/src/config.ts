/** Repository the atlas is generated from — provenance and footer links. */
export const REPO_URL = 'https://github.com/dnrohr/mathematical-structures';

/**
 * Major version of graph.json / search-index.json this app understands
 * (docs/graph-json.md). The loader refuses other majors and says so on
 * screen rather than rendering garbage (ARCHITECTURE.md §1).
 */
export const SUPPORTED_DATA_MAJOR = 1;

export const APP_TITLE = 'Structure Atlas';
