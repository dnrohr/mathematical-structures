/**
 * Placeholder until M3 (ROADMAP.md). The app workspace exists from M0 so the
 * toolchain and CI cover it from day one; the Vite scaffold, router, and
 * views arrive with the reader-app milestone.
 */
export const APP_NAME = 'Structure Atlas';
