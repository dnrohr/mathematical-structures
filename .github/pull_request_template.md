<!-- What does this PR change, and why? One or two sentences is plenty. -->

## Checklist

- [ ] `npm run check` passes locally (typecheck, lint, format, tests,
      content validation — the same gate CI runs).
- [ ] **New edges are reviewed as claims**: every added or changed edge in
      `graph/edges.yaml` is read aloud below with its qualifiers, e.g.
      "a linear-Gaussian SSM *is a* state-space model (theorem, exact)" —
      and none sounds overclaimed spoken.
- [ ] New concepts carry an honest `status` (`stub` and `hypothesis` are
      legal and visibly badged) and at least one edge or incoming link.
- [ ] Vocabulary changes (if any) touch only `graph/schema.yaml`, in their
      own commit.

## Edges in this PR, read aloud

<!-- Delete this section if the PR adds no edges. One line per claim:
- <from> —TYPE→ <to> (strength; context/caveats)
-->
