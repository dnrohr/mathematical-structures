{
  "name": "structure-atlas",
  "private": true,
  "description": "Workspace root for the Structure Atlas (see SPECIFICATION.md and ARCHITECTURE.md)",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "build",
    "app"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test --workspaces --if-present",
    "atlas": "npm run -w build atlas --",
    "dev": "npm run -w app dev",
    "build": "npm run clean && npm run build:data && npm run build:app",
    "build:data": "npm run -w build atlas",
    "build:app": "npm run -w app build",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\"",
    "budget": "node app/scripts/check-budget.mjs",
    "test:e2e": "npm run -w app test:e2e",
    "check": "npm run typecheck && npm run lint && npm run format:check && npm run test && npm run -w build atlas -- --check"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "eslint": "^9.34.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0",
    "vitest": "^3.2.4"
  }
}
